let tab = [
    {
        x: 1,
        y: 'object one value',
        operation: () => 'object one prafix' + this.x + this.y
    }
]

console.log(tab[0].operation()) // logs "object one prafixundefinedundefined" - dlaczego?
